<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bdqn.mapper.MgrinfoMapper">
    <select id="findList" resultType="Mgrinfo">
          SELECT * FROM mgrinfo WHERE  YEAR(buyDate)> 2015
    </select>
</mapper>
<!--
<mapper namespace="com.bdqn.entity.mapper.Smbms_providerMapper">
    <select id="count" resultType="_int">
        select count(1) from smbms_provider
    </select>
    <select id="list" resultType="com.bdqn.entity.Smbms_provider">
        select * from smbms_provider
    </select>
</mapper>
-->
